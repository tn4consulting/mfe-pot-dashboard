{
  "name": "dashboard",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/dashboard/src",
  "tags": ["type:app", "scope:dashboard"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "defaultConfiguration": "production",
      "outputs": ["{workspaceRoot}/dist/apps/dashboard"],
      "cache": true,
      "inputs": ["production", "^production"],
      "options": {
        "command": "node apps/dashboard/build/build.mjs",
        "cwd": "{workspaceRoot}"
      },
      "configurations": {
        "production": {
          "command": "node apps/dashboard/build/build.mjs"
        },
        "development": {
          "command": "node apps/dashboard/build/build.mjs --dev"
        }
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "command": "node apps/dashboard/build/serve.mjs",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/dashboard/jest.config.cts",
        "tsConfig": "apps/dashboard/tsconfig.spec.json"
      }
    },
    "serve-static": {
      "continuous": true,
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "dashboard:build",
        "staticFilePath": "dist/apps/dashboard/browser",
        "spa": true,
        "port": 4201
      }
    }
  }
}
